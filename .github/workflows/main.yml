name: Update AUR Package

on:
  schedule:
    - cron: '0 * * * *' # Controlla ogni ora
  workflow_dispatch: # Permette di lanciarlo manualmente

jobs:
  update-aur:
    runs-on: ubuntu-latest
    container: archlinux:base-devel # Usa Arch Linux per avere makepkg
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Install dependencies
        run: |
          pacman -Sy --noconfirm git curl jq
          # Crea un utente non-root chiamato 'build' e dagli i permessi di sudo
          useradd -m build
          echo "build ALL=(ALL) NOPASSWD: ALL" > /etc/sudoers.d/build
          # Permetti a build di scrivere nella cartella del workspace
          chown -R build:build .

      - name: Check for updates and update files
        id: check
        run: |
          # Esegui lo script come utente 'build'
          sudo -u build ./update.sh
        
      - name: Configure Git
        if: env.UPDATED == 'true'
        run: |
          git config --global --add safe.directory /__w/cockpit-tools/cockpit-tools
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Commit and Push to GitHub
        if: env.UPDATED == 'true'
        run: |
          git add PKGBUILD .SRCINFO
          git commit -m "Update to version ${{ env.VERSION }}"
          git push

      - name: Publish to AUR
        if: env.UPDATED == 'true'
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.AUR_SSH_PRIVATE_KEY }}" > ~/.ssh/aur_key
          chmod 600 ~/.ssh/aur_key
          
          cat <<EOF > ~/.ssh/config
          Host aur.archlinux.org
            User aur
            IdentityFile ~/.ssh/aur_key
            StrictHostKeyChecking no
          EOF
          
          mkdir aur-repo
          cd aur-repo
          
          git init
          git config user.name "${{ secrets.AUR_USERNAME }}"
          git config user.email "${{ secrets.AUR_EMAIL }}"
          
          git remote add origin "ssh://aur@aur.archlinux.org/cockpit-tools-bin.git"
          
          # Copia i file
          cp ../PKGBUILD ../.SRCINFO .
          
          # Aggiungi e committa
          git add PKGBUILD .SRCINFO
          git commit -m "Update to version ${{ env.VERSION }}"
          
          # Push forzato per la prima creazione o per allineare lo stato
          git push -f origin master
